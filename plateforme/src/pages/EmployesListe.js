import { Outlet } from "react-router-dom"

const EmployesListe = function() {

    return (
        <div>EmployesListe
            <Outlet/>
        </div>
        )
}

export default EmployesListe